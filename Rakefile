# vim: fileencoding=utf-8

require 'yard'

YARD::Rake::YardocTask.new do |t|
  t.options = [
    '--title', '汎用ダイスボット RGRB'
  ]
  t.files = [
    'lib/**/*.rb',
    '-',
    'doc/*.md',
    'doc/plugins/*.md'
  ]
end
